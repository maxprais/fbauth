extends layout

block content
    - if (name)
        p You are logged in as #{name}
    - else
        p You are not authorised
        p Click to go to login page
        a(href='/login'): input(class='login-btn', type='button' value='Take Me There')
